# The backend's API prefix (no trailing slash). Update for your running/deployed API.
VITE_API_BASE_URL=http://localhost:5000/api

# Optional: only change this if the backend uses a different login route.
VITE_AUTH_LOGIN_PATH=/auth/login

# Set to false when a real backend is available.
VITE_USE_MOCK_AUTH=true
